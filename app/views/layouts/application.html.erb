<!DOCTYPE html>
<html>
  <head>
    <title>KokudouStickerChecker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div>
      <% if admin_signed_in? %>
        <%= link_to "管理者ログアウト", destroy_admin_session_path, data: { turbo_method: :delete } %>
      <% elsif user_signed_in? %>
        <%= current_user.name %>さん、こんにちは！
        <%= link_to "ステッカー一覧", stickers_path %>
        <%= link_to "ユーザーログアウト", destroy_user_session_path, data: { turbo_method: :delete } %>
      <% else %>
        <%= link_to "ユーザーログイン", new_user_session_path %>
        <%= link_to "ユーザー登録", new_user_registration_path %>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
